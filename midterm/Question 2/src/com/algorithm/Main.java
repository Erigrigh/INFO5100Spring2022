package com.algorithm;

public class Main {

    public static void main(String[] args) {
	// write your code here
        // difference between abstract class and interface

        // methods
        // abstract class can have abstract method and non-abstract method
        // interface can only have abstract method;

        // variables
        // abstract class can have non-final final non-static static variables
        // interface can only have variables default by public;

        // inheritance
        // abstract class can only extends from another abstract class
        // interface can extends any number of other interface

        //implements
        //abstract class can implements different interface
        //interface can not implements other interface
    }
}
